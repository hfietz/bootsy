{% extends '::base.html.twig' %}

{%  block dialog %}
    <h2>Database versions:</h2>
    {%  if versions is defined and versions %}
        <table id="versions">
            <thead>
                <tr><th>File</th><th>Version</th><th>Date</th><th>Installed Version</th><th>Installed Date</th><th>Status</th></tr>
            </thead>
            <tbody>
            {%  for version in versions %}
                {% spaceless %}
                <tr class="{{ versions.status }}">
                    <td>{{ versions.file }}</td>
                    <td>{{ versions.currentVersion }}</td>
                    <td>{{ versions.lastChanged|date('Y-m-d H:i:s') }}</td>
                    <td>{{ versions.lastVersionRun }}</td>
                    <td>{{ versions.lastTimeRun|date('Y-m-d H:i:s') }}</td>
                    <td>{{ versions.status }}</td>
                </tr>
                {% endspaceless %}
            {%  endfor %}
            </tbody>
        </table>
    {%  else  %}
        <p>No version info is available. There probably are no version scripts yet.</p>
    {%  endif %}
{% endblock %}